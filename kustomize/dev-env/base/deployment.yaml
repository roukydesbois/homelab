apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-env
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-env
  strategy:
    type: Recreate  # Only one pod at a time — PVC is ReadWriteOnce
  template:
    metadata:
      labels:
        app: dev-env
    spec:
      containers:
        - name: dev-env
          image: ghcr.io/roukydesbois/dev-env:latest@sha256:99f5c655779f43f1714c925986d0ac3c0156bdd87883b8cf44c64395f88b80ca
          imagePullPolicy: Always
          ports:
            - containerPort: 22
          volumeMounts:
            - name: home
              mountPath: /home/thomas
            - name: authorized-keys
              mountPath: /etc/ssh/authorized_keys/thomas
              subPath: thomas
              readOnly: true
      volumes:
        - name: home
          persistentVolumeClaim:
            claimName: dev-env-home
        - name: authorized-keys
          secret:
            secretName: dev-env-authorized-keys
