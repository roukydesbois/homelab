apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ts-n8n
spec:
  defaultBackend:
    service:
      name: n8n
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - n8n
